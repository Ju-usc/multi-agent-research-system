[project]
name = "multi-agent-research-system"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dspy>=3.0.0b1",
    "openai",
    "jupyter",
    "pydantic",
    "brave-search-python-client",
    "wikipedia",
    "nest-asyncio>=1.6.0",
] 
